name: manager_api
description: Manager GraphQL and Rest
version: 1.5.7
publish_to: 'none'
homepage:

environment:
  sdk: '>=3.5.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.9.2
  cross_file: ^0.3.5
  log_print: ^0.0.2
  web_socket_channel: 3.0.3
  web: ^1.1.1
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
