name: manager_api
description: Manager GraphQL and Rest
version: 0.0.8
publish_to: 'none'
homepage:

environment:
  sdk: '>=3.1.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.1.0
  graphql: ^5.0.1
  collection: ^1.17.1
  gql: ^1.0.0
  open_filex: ^4.3.4
  rxdart: ^0.27.7
  http_parser: ^4.0.2
  dio: ^5.3.3
  universal_html: ^2.2.4
  cross_file: ^0.3.3+6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
